export const CONFIG = {
	/** Warn when a .env file's permissions let group or others read it. */
	warnOnLoosePermissions: true,
	/** Print a one-line summary of what was loaded at startup. Names only, never values. */
	announce: true,
};
